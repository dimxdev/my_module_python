"""
   1. cara mengetahui comand = pip
   2. cara nge check versi = pip --version
   3. cara mengecheck isi pip = pip list
   4. cara install package = pip install (nama package)
   5. cara install package(versi detail) = pip install (nama package)==(versi)
   6. cara uninstall package = pip uninstall (nama package)
"""